# ---- Public (safe in the browser bundle) ----
NEXT_PUBLIC_SUPABASE_URL=https://wmifzadxkmjweyvffggf.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_OCjrnUezsb2GDkhOTEUodQ_SoIIeqeG
NEXT_PUBLIC_SITE_URL=https://www.fyrecore.app

# ---- SECRET. Server only. Never prefix with NEXT_PUBLIC_. ----
# Bypasses row level security entirely. Production only, never Preview:
# preview URLs are publicly reachable.
SUPABASE_SERVICE_ROLE_KEY=

# Shared secret the game server signs match reports with. openssl rand -hex 32
GAME_SERVER_SECRET=
# ---- Didit identity verification (secret, server only) ----
# https://business.didit.me -> API key, webhook secret, and the workflow id for the KYC flow you configure
DIDIT_API_KEY=
DIDIT_WEBHOOK_SECRET=
DIDIT_WORKFLOW_ID=

# ---- Stripe (secret, server only) ----
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
